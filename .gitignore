# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Synced from pikku repo
/docs/skills
/skills

# Screenshots
/*.png

# Playwright MCP
.playwright-mcp/

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.yarn/*
!.yarn/releases
!.yarn/plugins
.pnp.*

# Screenshot sweep (scripts/screenshots.mjs) — big, regenerable, not versioned
/screenshots

# Impeccable critique snapshots
/.impeccable

# Per-spec detail files for the OpenAPI catalogue: 20MB of generated input to
# plugins/openapi-catalogue.js, rebuilt by `npm run sync-registry`.
# src/data/openapi-spec-meta.json is the cache that keeps that rebuild to
# minutes rather than an hour, and IS tracked.
/src/data/openapi-specs/
